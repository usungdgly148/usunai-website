# 回退流程

## 代码回退

1. 确认故障首次出现在哪个发布版本。
2. 优先撤销导致问题的独立提交，而不是恢复整台服务器。
3. 紧急情况下检出上一个已验证标签并重新发布代码文件。
4. 不回滚数据库，除非明确存在数据库迁移故障并已有对应恢复方案。

## 服务器文件回退

1. 使用本次发布前创建的精确文件备份。
2. 只恢复本次被覆盖的代码或配置文件。
3. 重启 `usun.service`。
4. 重复发布后验证清单。

## 数据恢复

数据库、上传文件和向量数据不通过 Git 回退。数据事故应使用腾讯云/COS/数据库备份恢复流程，并在恢复前保留故障现场副本。
