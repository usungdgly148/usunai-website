# 阶段 D：全量回归与候选发布验收

更新日期：2026-08-20

## 自动回归结果

恢复分支已经完成以下自动化验证：

1. 仓库敏感信息扫描、基线检查和服务端语法检查；
2. 阶段 A 至 D 的功能契约检查；
3. 临时 SQLite 后端集成测试，覆盖注册、双会话登录、跨会话历史、资产保存、管理员 Token 隔离、套餐充值、有效期、算力流水、订单和退出登录；
4. 前端干净依赖安装和生产构建；
5. 生产公共接口与首页的只读健康检查。

GitHub Actions 运行 `32361496629` 已通过。候选标签 `candidate-recovery-stage-d-2026-08-20` 指向提交 `a2e916d`，没有触发生产部署。

## 隔离候选

阶段 D 前端候选构建位于本地忽略目录 `.tmp/candidates/`，没有覆盖仓库 `dist/` 或生产文件。阶段 E 会将候选包扩展为前端、后端和部署配置的完整交付物。

## 仍需人工验收

- 桌面、iOS Safari、Android Chrome 的登录、聊天、工作流、资产和历史交互；
- Coze 新版、Coze 旧版、DeepSeek 原生模型各执行一次真实任务；
- DeepSeek 思考过程、首字耗时、总耗时、错误率和成本；
- 百炼向量、知识库上传/替换/删除、RAG 命中与回答质量；
- 管理后台套餐充值、手动调整、算力流水和订单的真实账号一致性。

以上项目未通过前，不启用自动生产部署。
