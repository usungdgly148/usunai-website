{
  "name": "usunai-website-baseline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:secrets": "node scripts/check-secrets.mjs",
    "check:baseline": "node scripts/verify-baseline.mjs",
    "check:stage-a": "node scripts/check-stage-a-contracts.mjs",
    "check:stage-b": "node scripts/check-stage-b-contracts.mjs",
    "verify": "npm run check:secrets && npm run check:baseline && npm run check:stage-a && npm run check:stage-b"
  }
}
