# 变更规则

## 开始任务

1. 从最新 `main` 创建 `fix/<简短名称>` 或 `feature/<简短名称>` 分支。
2. 只修改当前任务必要的文件。
3. 不提交数据库、上传文件、备份或任何真实凭证。

## 提交前

```bash
npm run verify
git diff --check
git status --short
```

提交信息使用明确的中文动词，例如：

```text
修复：资产保存请求超过服务端限制
优化：手机端工作流参数面板高度
新增：原生 DeepSeek 智能体配置
```

## 合并与发布

- 一个提交尽量只解决一个明确问题。
- 不把多个无关修复揉在一次提交中。
- 合并到 `main` 前必须通过自动检查。
- 发布后创建 Git 标签并记录验证结果。
