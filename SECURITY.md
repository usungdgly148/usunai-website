# 安全规则

## 禁止提交

- `.env` 和真实环境配置
- API Key、Token、Cookie、Authorization 值
- OAuth、TLS、SSH 私钥
- 数据库、WAL、SHM、向量数据和快照
- 用户上传文件、知识库原文和运行日志
- 生产备份与部署前临时副本

## 如果发现凭证进入 Git 历史

1. 立即停止推送和部署。
2. 不只删除当前文件，因为旧提交仍保留该内容。
3. 轮换对应凭证。
4. 清理 Git 历史后重新扫描。
5. 验证旧凭证已经失效。

不要在 Issue、提交信息、日志或截图中粘贴真实凭证。
